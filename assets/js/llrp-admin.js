jQuery(document).ready(function ($) {
  $(".llrp-color-field").wpColorPicker({
    palettes: true, // mantém paleta
    clear: true, // botão Limpar
    hide: true, // esconde após seleção
    showAlpha: true, // habilita o controle de transparência
  });
});
